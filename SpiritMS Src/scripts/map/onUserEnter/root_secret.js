function start(ms) {
if (ms.hasQuest(30000));
    ms.sendDelay(3000);
    
} 
if (ms.hasQuest(30003)); {
    ms.setPlayerAsSpeaker()
    ms.sendNext("Hmm.. It seems to work fine. Let's go back to tell her about the exit.");
    ms.dispose;
} else {
    ms.dispose();
    ms.setPlayerAsSpeaker();
    ms.sendNext("This fog is too thick! I gotta keep my senses sharp. Who knows what's lurking out there...");
}
    